<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose owns all theming; these styles only shape the window before Compose attaches. -->
    <style name="Theme.Orwyx" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>

    <!-- Player window: black, edge-to-edge, keeps the screen on while attached. -->
    <style name="Theme.Orwyx.Player" parent="Theme.Orwyx">
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>
